# Validates: REQ-F-CHAN-001
"""pytest configuration for genesis_chat tests."""
import sys
from pathlib import Path

# Ensure src is on the path
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))
